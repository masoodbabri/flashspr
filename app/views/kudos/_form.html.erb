<%= form_for([@staff, @staff.kudos.build]) do |f| %>
    <p>
      <%= f.label :Message %><br>
      <%= f.text_area :message %>
    </p>
    <p>
      <%= f.submit 'submit', class: 'green ui button'%>
    </p>
<% end %>